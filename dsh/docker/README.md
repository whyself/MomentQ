# MomentQ DSH Docker Runtime (planned)

The Docker image will package the pinned DSH native framework and MomentQ runtime. It must bind-mount the complete logical data root and run the loopback Web Profile in the foreground. Its implementation is tracked as Task 10 in the DSH layer plan.
