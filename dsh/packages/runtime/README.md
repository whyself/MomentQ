# MomentQ DSH Runtime (planned)

The runtime package will pin the native DSH CLI and plugins, load `../bundle`, validate `MOMENTQ_DATA_ROOT` and `DSH_HOME`, and expose a user-facing local start command. Its implementation is tracked as Task 10 in the DSH layer plan.
